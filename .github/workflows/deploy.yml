name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: denoland/setup-deno@v2.0.5
        with:
          deno-version: v2.x

      - name: Install dependencies
        working-directory: climb-timer
        run: deno install

      - name: Build
        working-directory: climb-timer
        run: deno task build

      - uses: actions/upload-pages-artifact@v5
        with:
          path: climb-timer/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/deploy-pages@v5
        id: deployment
